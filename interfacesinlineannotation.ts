//Inline Annotation 

let drawPoint=(point:{x:number,y:number})=>{

}

drawPoint({
    x:1,
    y:2
})

///////////////////////////////////////////////////////////////////////

//Interfaces Implementation for clean code and declaration of the structure of the function

interface Point{
    x:number,
    y:number
}

let IdrawPoint=(point:Point)=>{

}